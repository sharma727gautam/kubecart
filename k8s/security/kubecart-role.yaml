apiVersion: rbac.authorization.k8s.io/v1

kind: Role

metadata:
  name: kubecart-reader
  namespace: kubecart

rules:

  - apiGroups: [""]

    resources:
      - pods
      - services
      - configmaps

    verbs:
      - get
      - list
      - watch