apiVersion: v1

kind: PersistentVolumeClaim

metadata:
  name: postgres-pvc
  namespace: kubecart

spec:

  accessModes:
    - ReadWriteOnce

  storageClassName: local-storage

  resources:
    requests:
      storage: 1Gi