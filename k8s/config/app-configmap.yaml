apiVersion: v1

kind: ConfigMap

metadata:
  name: kubecart-config
  namespace: kubecart

data:

  APP_ENV: "development"

  LOG_LEVEL: "INFO"

  FEATURE_DISCOUNT_ENABLED: "true"
  SPRING_DATASOURCE_URL: "jdbc:postgresql://postgres-service:5432/kubecart"
  SPRING_DATASOURCE_USERNAME: "postgres"